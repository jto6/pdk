#
# This file is the makefile for building UDMA memcpy tricore test app for TI RTOS
#
ifeq ($(RULES_MAKE), )
include $(PDK_INSTALL_PATH)/ti/build/Rules.make
else
include $(RULES_MAKE)
endif

APP_NAME = udma_memcpy_tricore_testapp_$(BUILD_OS_TYPE)

include makefile.mk
